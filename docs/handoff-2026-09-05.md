# Handoff — 2026-09-05 (evening) — EC full run on opus, the 12 fixes, Tier C landed

ต่อจาก handoff เช้าวันเดียวกัน (Phase A/B/C ของ commerce-agents) ซึ่งถูกแทนด้วยไฟล์นี้

## สถานะปัจจุบัน

- Branch `phase-abc-action-authority`, head `5788263`, **push แล้ว** เป็น `master` ของ https://github.com/aeiouboy/wowlidator (remote `mine`, upstream ตั้งไว้) — `git status -sb` = `## phase-abc-action-authority...mine/master` ไม่มี source/test ค้าง
- `origin` = AruzCandlr/wowlidator อ่านอย่างเดียว (403) ยังไม่ได้เปิด PR
- ตรวจที่ head (รันจริงเวลา ~21:00): `npm run typecheck` ผ่าน, `npx tsc -p tsconfig.test.json --noEmit` ผ่าน, `npm run build` ผ่าน, ทุก test file แบบไม่แตะ browser (`WOWLIDATOR_CDP_URL=http://127.0.0.1:1 npx tsx --test tests/*.test.ts`) **2,207/2,208 ผ่าน** — ตัวเดียวที่พังคือ `tests/value-resolution.test.ts` "the date grammar the second pass needed" (`วันที่ก่อน Hire Date 1 ปี` → undefined) **พังอยู่ก่อนแล้วที่ e491b1d** ยืนยันด้วย worktree ชั่วคราว ไม่ใช่ของวันนี้
- browser tier ยังไม่ได้รันกับโค้ดชุดใหม่ (กฎผู้ใช้: ไม่ใช้ shared browser เว้นแต่สั่ง)

## งานที่เสร็จใน session นี้

### การรัน EC catalog จริง (humi SIT, claude-cli)
- ไฟล์: `/Users/tachongrak/Projects/cnext-azure/outputs/20260904-143358/QA_Task_Tracking_Cycle1_SIT_Optimized_EC.csv` 272 เคส / 309 planned; personas automate01/automate02 (รหัสใน `.env` → `WOWLIDATOR_AS`, `WOWLIDATOR_PERSONAS`); คำสั่งเต็มอยู่บรรทัด 3 ของ `.wowlidator/reports/catalogs/ec-full-run-opus.log`; log รอบ 1–2 เก็บเป็น `ec-full-run-opus-attempt1.log`, `-attempt2.log`
- ledger `.wowlidator/reports/catalogs/qa-task-tracking-cycle1-sit-optimized-ec-csv.claims.progress.json`: ผลจริง 67 เคส (passed 3, passed-with-issues 1, failed 6, error จริง ~25, dead-end 2, needs-review 1, authoring refused ~28) และ **243 เคสเป็นพิษจากโควตา** (session window ชน 100% เวลา 17:07 แล้ว process วิ่งต่อ 2 ชม. ด้วย refusal 1,813 ครั้ง): 213 บันทึก never-ran (resume รันใหม่เอง), **30 บันทึก error (ต้อง `--rerun-errors`)**
- probe อ่าน API ของ humi ตรง (`.wowlidator/probe/probe*.mts`, ผลใน scratchpad ของ session): Position ทั้ง 100 รหัสในชีตมีจริงและ vacant 358/360 แถว; Company C005 มีจริง; **ตัวปัญหาคือ picker หน้า New Hire โหลดแค่ 500 แถวแรกต่อบริษัท (`foundation?type=positions&page=1&size=500`) และค้นด้วยชื่ออย่างเดียว** — 29 รหัส (151 แถว) อยู่นอก 500 นั้น รวม 40106337 (77 แถว) → เป็น defect/ข้อจำกัดของ humi ต้องแจ้งทีมแอป ไม่ใช่ data หรือสิทธิ์ของ automate

### 7 commit แก้ bug + ลด token (e573f1a..e491b1d)
- `e573f1a` โหลด `.env` ก่อนอ่าน `--as`/`--persona` (308 เคสถูกปฏิเสธรอบแรก)
- `6c8f150` `routeSectionOf`: base path หน้า locale ไม่ใช่ section (ทุกหน้า `/humi/th/...` เคยรวมเป็น `route:humi/th` เลนรอ lock 20–26%)
- `6414e6e` `baseUrlOf` + `resolveUrl`: request step เก็บ `/humi` (404 ปลอม 27 ครั้ง 6 เคส consent) + prompt บอกใช้ retrieval ≤ 2 ครั้ง
- `e8d97dc` รายงานจัดกลุ่มรหัสมีขีด / scenario จาก bundle (เลิก ungrouped 269)
- `00a31d6` warm slot ใช้ซ้ำ (cold call 22% หาย) + retrieval 4 hits × 800 chars
- `519333d` fail-fast floor ตามจำนวนช่องใน goal (`FORM_LEG_TURN_SLACK`), `listboxCannotOffer` รับ trigger label ข้ามภาษา, listbox `uniquePrefixMatch` ("Thai" → "Thailand - Thailand")
- `e491b1d` **`src/cli/quota-hold.ts`** หยุด dispatch ที่ 85% ของ session (`WOWLIDATOR_QUOTA_HOLD_PERCENT`) แล้วปล่อยเมื่อ reset; risk judge ไม่ re-author เคส expected-fail

### Tier C ผ่าน swarm (5 builder + 5 blind verifier, ผ่านรอบแรกทั้งหมด) แล้วรวมทีละ branch
- `002d655` C8 bench: `src/bench/`, `npm run bench` (gate `WOWLIDATOR_BENCH=1`), fixtures `tests/fixtures/bench/`
- `a9b432c` C5 master-data: `src/context/master-data.ts`, `wowlidator data check <catalog> --master-data <file> --url <app> [--json]`
- `e0c48b6` fix หลังรวม C5: import `acceptConsentGate` จาก `consent-gate.js`; test รันจาก scratch cwd (ไม่อ่าน `.env` ของเครื่อง)
- `b7a62b3` C4 findings: `src/reporter/findings.ts`, `findings-export.ts` (`<report>-findings.md/.xlsx`), never-ran พับเป็นก้อนเดียว
- `d0ccf53` C1 resume จาก flow ที่แต่งแล้ว: `SuiteLedger.authored`, `reusableAuthored`, `FlowFileSchema`
- `eb7d47c` C3 typed stop reason: `AgentRecord.endedBy`, `AgentAction.listbox`, `AgentRecord.unreachable`, `AgentBudgetError`/`AgentEvidenceError` (cannot-offer → `failed` มี expected/actual), `harnessOnly` พูด "no verdict — the agent's own account, unverified"
- **ข้อควรรู้**: worktree ของ swarm แตกจาก `main` (9cd16b6) ไม่ใช่ branch เรา — git รวม 3 ทางผ่านหมด แต่โค้ดถูกเขียนกับ layout เก่า (เจอ 1 จุดแล้วแก้) ควรรัน browser tier กับเคสจริง 2–3 เคสก่อน resume เต็ม

### `.env` ปัจจุบัน (ไม่ commit)
generator claude-cli/opus effort medium; agent, healer, data claude-cli/**sonnet** effort low; `WOWLIDATOR_AS`, `WOWLIDATOR_PERSONAS`; `WOWLIDATOR_MUTATION_POLICY` allow submit/delete/approve + approved delete/approve `*`. สำรอง env เดิม `.env.backup-before-opus-ec`

## ค้างอยู่ / ยังไม่ commit
- `docs/handoff-2026-09-05.md` (ไฟล์นี้) และ `docs/research/2026-09-05-explore/*.md` (รายงานสำรวจ 6 ฉบับ + `improvement-plan-2026-09-05.md`) — `docs/` อยู่นอก sparse checkout: `git add --sparse docs/handoff-2026-09-05.md docs/research/2026-09-05-explore && git commit -m "docs: handoff and exploration reports 2026-09-05"`
- `.claude/worktrees/wf_581a4984-9b4-{1..5}` — worktree ของ swarm ที่รวมแล้ว ลบได้: `for i in 1 2 3 4 5; do git worktree remove --force .claude/worktrees/wf_581a4984-9b4-$i; git branch -D worktree-wf_581a4984-9b4-$i; done`
- `.wowlidator/probe/*.mts` (probe scripts, untracked, มีประโยชน์ไว้ probe รอบหน้า) — เก็บหรือลบก็ได้
- ไม่มี source/test ค้าง

## Services ที่รันอยู่
- Chrome pool ของ EC run ที่ `http://127.0.0.1:9333` (+9334–9340, profile `/tmp/wowlidator-ec-full-9333`) **ยังเปิดค้าง** — resume จะต่อเข้าตัวเดิม; ปิดด้วย `pkill -f "remote-debugging-port=93"`
- Chrome dev ที่ 9222 ก็เปิดอยู่ (ของ wowlidator เอง)
- ไม่มี catalog run, ไม่มี panel, ไม่มี Codex/GLM

## Next steps (เรียงตามความสำคัญ)
1. **smoke browser tier กับโค้ดชุดใหม่** 2–3 เคส ก่อน resume เต็ม: `npm run cli -- catalog <csv> --url https://humi-sit-int.central.co.th/humi/th/login --repo humi-5b36 --run --rerun-case CNS-EC-029 --rerun-case HIR-EC-006 --resume --cdp http://127.0.0.1:9333 --chrome-profile /tmp/wowlidator-ec-full-9333 --headless --screenshots on-event --video on` แล้วดูว่า consent API ไปที่ `/humi/api/...` และ leg ที่ล้มมี `endedBy`
2. **resume เต็ม**: คำสั่งเดิม (log บรรทัด 3) + `--resume --rerun-errors` + `--author-concurrency 8`; เช็คโควตาก่อน `npx tsx .wowlidator/probe/quota.mts` (ตอนปิด session: session 35%, week 37%); ดูบรรทัด `⏸ quota hold` ใน log; งบ: ก่อนแก้ 67 ผลจริง ≈ 26% ของสัปดาห์ หลังแก้คาดว่าลดราวครึ่ง
3. **แจ้งทีม humi**: Position picker โหลด 500/5,364 ของ C001 และค้นด้วยชื่อ → 29 รหัส/151 แถวเลือกไม่ได้จาก UI (ตัวเลขจาก probe5/probe6); ใช้ `wowlidator data check` เมื่อมีไฟล์ประกาศ lookup (ตัวอย่างใน `tests/fixtures/master-data.lookups.json`)
4. Tier C รอบ 2 (ยังไม่ทำ): C6 response body จาก network ของหน้าเอง (ต่อยอด C3), C7 induced skills/row template (ต่อยอด C5), pre-flight `blocked (data)` และ authoring hook ของ master-data (explore-data-grounding ข้อ 1–2)
5. แก้ test ที่พังอยู่ก่อน: `tests/value-resolution.test.ts` date grammar `วันที่ก่อน Hire Date 1 ปี`
6. เปิด PR เข้า AruzCandlr ถ้าต้องการ: `gh repo fork AruzCandlr/wowlidator --remote --remote-name fork && git push -u fork phase-abc-action-authority`

## Gotchas
- **โควตา session เต็มแล้ว process ไม่หยุด (ก่อน quota hold)** → เคสที่เหลือถูกบันทึกเป็น never-ran/error ด้วย refusal; `--resume` เก็บ never-ran คืน แต่ error ต้อง `--rerun-errors`
- Claude Code auto-mode classifier บล็อกคำสั่ง `npm run cli` ยิง SIT บ่อย — ให้ผู้ใช้ตอบ "รันได้" แล้วยิงซ้ำ หรือผู้ใช้พิมพ์ `!` นำหน้าเอง; ห้ามใส่รหัสผ่านใน argv (ใช้ `.env`)
- `--author-concurrency 8` + 8 lanes เกิน warm pool 9 → เคยตก cold 22% (แก้แล้วที่ `00a31d6`)
- คำเตือน "hard code": ระบบเป็น general QA verification — ห้ามมีชื่อแอป/ป้ายไทย/รหัสใน `src/` (ตรวจด้วย `git diff -U0 -- src | grep -E "^\+" | grep -iE "humi|ตำแหน่ง|C001"`)
- `tests/cli.test.ts` subprocess รันจาก repo root จะอ่าน `.env` ของเครื่อง — test ที่ต้อง "ไม่มี credential" ต้องใช้ scratch cwd
- zsh: `$VAR` ไม่ word-split → `setopt shwordsplit` ก่อนส่ง list ไฟล์ให้ `npx tsx --test`; `echo ====` error ใช้ `echo '---'`; macOS ไม่มี `timeout`
- `npx tsc -p tsconfig.test.json` ไม่มี DOM lib; `src/ui/wow-ui-html.ts` ห้าม backtick ในคอมเมนต์

## Key files/docs
- แผนรวม: `docs/research/2026-09-05-explore/improvement-plan-2026-09-05.md` (ชั้น A/B/C); รายงานสำรวจ 6 ฉบับข้าง ๆ
- กฎ agent ใหม่: `src/orchestrator/CLAUDE.md` section "Why the leg ended is data"; findings: `src/reporter/CLAUDE.md`; master-data: `src/context/CLAUDE.md`; quota hold: root `CLAUDE.md` (bullet src/cli) + `.env.example`
- โค้ดใหม่: `src/cli/quota-hold.ts`, `src/reporter/findings.ts`, `src/reporter/findings-export.ts`, `src/context/master-data.ts`, `src/bench/`, `src/cli/suite-progress.ts` (`authored`, `reusableAuthored`)
- memory: `~/.claude/projects/-Users-tachongrak-Projects-QA-Agent-wowlidator/memory/wowlidator-ec-opus-run-2026-09-05.md`
